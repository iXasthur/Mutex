using System;

namespace Mutex
{
    class Program
    {
        static void Main(string[] args)
        {
            Mutex mutex = new Mutex();
            OSHandle handle = new OSHandle();
        }
    }
}